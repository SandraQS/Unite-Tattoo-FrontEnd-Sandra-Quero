export type GeneralButtonProps = {
  textButton: string;
  functionOnPress: () => void;
};

export type TalentCard = {
  imageWork: string;
  titleWork: string;
  subtitleWork: string;
  functionOnPress?: () => void;
};
