import { StyleSheet } from "react-native";

export default StyleSheet.create({
  allWorkList: {
    flex: 1,
    flexDirection: "row",
    flexWrap: "wrap",
    justifyContent: "space-between",
  },
});
