import { StyleSheet } from "react-native";

export default StyleSheet.create({
  collections: {
    flex: 1,
    flexDirection: "row",
    flexWrap: "wrap",
    justifyContent: "space-between",
    marginTop: 20,
  },
});
